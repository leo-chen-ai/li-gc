## ADDED Requirements

### Requirement: Worker Action Menu

The project worker table SHALL present row operations in a menu-style interaction that can hold more actions without widening the table.

#### Scenario: Download from worker action menu

- **GIVEN** a worker row in project detail
- **WHEN** the admin opens the row action menu
- **THEN** contract template download is available alongside edit/delete actions
