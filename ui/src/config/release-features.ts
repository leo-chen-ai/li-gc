// Complete but unreleased features. Keep these disabled until the coordinated
// product release exposes all three entry points together.
export const releaseFeatures = {
  attendanceGeofence: false,
  faceLibrarySync: false,
  extendedReportSources: false,
} as const;
